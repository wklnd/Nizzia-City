// CARTEL / DRUG EMPIRE CONFIG
// Static data lives in cartel.json — this file loads it and exports everything,
// plus builds the derived TERRITORIES flat-lookup map.

const data = require('../data/cartel.json');

const {
  DRUGS,
  LABS,
  REGIONS,
  NPC_ROLES,
  NPC_STATS,
  NPC_RARITIES,
  NPC_HIRE_COSTS,
  NPC_FIRST_NAMES,
  NPC_LAST_NAMES,
  NPC_XP_PER_LEVEL,
  NPC_MAX_LEVEL,
  NPC_LOYALTY_DECAY,
  NPC_LOYALTY_GAIN_MISSION,
  NPC_LOYALTY_MAX,
  NPC_BETRAY_THRESHOLD,
  MISSION_TYPES,
  COMBAT_WEIGHTS,
  DEFENDER_BONUS,
  CASUALTY_CHANCE_LOSS,
  CASUALTY_CHANCE_WIN,
  INJURY_CHANCE_WIN,
  INJURY_RECOVERY_HOURS,
  CARTEL_CREATE_COST,
  CARTEL_MAX_NPCS_BASE,
  CARTEL_MAX_NPCS_PER_REP,
  CARTEL_MAX_LABS_BASE,
  CARTEL_MAX_LABS_PER_REP,
  REP_LEVELS,
  PASSIVE_REP_PER_LAB,
  PASSIVE_REP_PER_TERRITORY,
  PASSIVE_REP_PER_NPC,
  PASSIVE_REP_BASE,
  HEAT_DECAY_PER_HOUR,
  HEAT_PER_PRODUCTION,
  HEAT_PER_SALE,
  HEAT_PER_DELIVERY,
  HEAT_PER_ATTACK,
  HEAT_PER_SEIZE,
  HEAT_PER_ASSASSINATION,
  HEAT_PER_SABOTAGE,
  HEAT_PER_SMUGGLING,
  HEAT_PER_INTIMIDATION,
  HEAT_CORRUPTION_REDUCE,
  HEAT_BUST_THRESHOLD,
  HEAT_BUST_CHANCE_PER_POINT,
  BUST_PRODUCT_LOSS,
  BUST_MONEY_LOSS,
  BUST_NPC_ARREST_CHANCE,
  BUST_COOLDOWN_HOURS,
  TERRITORY_UPGRADES,
} = data;

const TERRITORIES = {};
for (const region of Object.values(REGIONS)) {
  for (const terr of Object.values(region.territories)) {
    TERRITORIES[terr.id] = { ...terr, region: region.id, regionName: region.name };
  }
}

module.exports = {
  DRUGS,
  LABS,
  REGIONS,
  TERRITORIES,
  NPC_ROLES,
  NPC_STATS,
  NPC_RARITIES,
  NPC_HIRE_COSTS,
  NPC_FIRST_NAMES,
  NPC_LAST_NAMES,
  NPC_XP_PER_LEVEL,
  NPC_MAX_LEVEL,
  NPC_LOYALTY_DECAY,
  NPC_LOYALTY_GAIN_MISSION,
  NPC_LOYALTY_MAX,
  NPC_BETRAY_THRESHOLD,
  MISSION_TYPES,
  COMBAT_WEIGHTS,
  DEFENDER_BONUS,
  CASUALTY_CHANCE_LOSS,
  CASUALTY_CHANCE_WIN,
  INJURY_CHANCE_WIN,
  INJURY_RECOVERY_HOURS,
  CARTEL_CREATE_COST,
  CARTEL_MAX_NPCS_BASE,
  CARTEL_MAX_NPCS_PER_REP,
  CARTEL_MAX_LABS_BASE,
  CARTEL_MAX_LABS_PER_REP,
  REP_LEVELS,
  PASSIVE_REP_PER_LAB,
  PASSIVE_REP_PER_TERRITORY,
  PASSIVE_REP_PER_NPC,
  PASSIVE_REP_BASE,
  HEAT_DECAY_PER_HOUR,
  HEAT_PER_PRODUCTION,
  HEAT_PER_SALE,
  HEAT_PER_DELIVERY,
  HEAT_PER_ATTACK,
  HEAT_PER_SEIZE,
  HEAT_PER_ASSASSINATION,
  HEAT_PER_SABOTAGE,
  HEAT_PER_SMUGGLING,
  HEAT_PER_INTIMIDATION,
  HEAT_CORRUPTION_REDUCE,
  HEAT_BUST_THRESHOLD,
  HEAT_BUST_CHANCE_PER_POINT,
  BUST_PRODUCT_LOSS,
  BUST_MONEY_LOSS,
  BUST_NPC_ARREST_CHANCE,
  BUST_COOLDOWN_HOURS,
  TERRITORY_UPGRADES,
};